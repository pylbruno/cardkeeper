rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 使用者只能讀寫自己 uid 底下的卡片資料
    match /users/{userId}/cards/{cardId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // 其他路徑一律拒絕
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
